import { Layout } from 'antd'
import styled from 'styled-components'

const StyledLayout = styled(Layout)`
  position: relative;
  min-height: 100vh;
  color: #000;
  overflow: hidden;
`
export { StyledLayout }
